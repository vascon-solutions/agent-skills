# Status Model

Use the repo's existing status vocabulary when present.

If none exists, prefer a small durable set:

- `todo`
- `in-progress`
- `blocked`
- `done`
- `deferred`
- `cancelled`

Keep statuses mutually exclusive.
Avoid custom statuses unless they carry clear operational value.

