interface:
  display_name: "Task Doc Delivery Loop"
  short_description: "Deliver approved task docs to a draft PR or safe local fallback"
  default_prompt: "Use $task-doc-delivery-loop to deliver the selected approved task doc or ordered task-doc set through implementation, validation, review, and one draft PR when GitHub PR delivery is available; otherwise report verified local completion. Do not start ongoing monitoring unless requested."
