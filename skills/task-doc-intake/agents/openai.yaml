interface:
  display_name: "Task Doc Intake"
  short_description: "Ground intended changes in a delivery task"
  default_prompt: "Use $task-doc-intake to ground this intended change in code, resolve only missing scope decisions, and produce the task doc. Preserve existing approval and hand off to implementation only when already requested."
